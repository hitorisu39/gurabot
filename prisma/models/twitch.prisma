model OsuToTwitch {
    osuID      Int          @id @map("osu_id")
    twitchID   String       @unique @map("twitch_id")

    createdAt  DateTime     @map("created_at") @default(now())
    updatedAt  DateTime     @map("updated_at") @updatedAt @default(now())

    @@map("osu_to_twitch")
}